import React from 'react';

const ReportFormattingForm = (props) => (
    <p style={{ 'height': '35em', 'marginBottom': '15px' }}>
        { props.text }
    </p>
);

export default ReportFormattingForm;
